[project]
name = "hexagonal-chess"
version = "0.0.1"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-mock",
    "httpx",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
